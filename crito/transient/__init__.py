"""CRITO transient follow-up pipeline.

Broker ingest (ALeRCE) → visibility filtering (astropy) → candidate list →
supervisor approval (Slack/email) → observation request → execution sequencer.

See docs/plan/12-TRANSIENT-FOLLOWUP-PLAN.md.
"""
